build:
  web:
    command: npm install && npm run build
  backend:
    command: pip install -r requirements.txt

run:
  web: npm start  # or whatever command you use to start your frontend application
  backend: uvicorn src.app:app --host 0.0.0.0 --port $PORT

addons:
  - plan: heroku-postgresql
    as: DATABASE
